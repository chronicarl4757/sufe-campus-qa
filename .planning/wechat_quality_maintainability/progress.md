# 进度日志

## 2026-08-20

### 阶段 1：文章与仓库现状

- **状态：** in_progress
- 已读取本次工作规范、Ponytail 与文件规划技能。
- 已恢复根目录旧规划，确认其属于先前采集任务。
- 已检查 Git 状态，确认工作区包含大量未提交用户改动；不回退、不覆盖。
- 普通网页读取公众号链接无正文；浏览器运行时可用列表为空，已记录并切换到本地证据/只读 HTTP 路径。
- 用户打开文章后再次尝试扩展与目标 URL 发现，仍无浏览器连接；精确公网搜索也无结果，停止重复同一路径。
- 已读取直接引用大赛公告的已批准设计规格、当前 README/依赖/环境示例，以及 CLI/配置入口；确认公告评审顺序与用户本次优先级一致。
- 已读取质量审计/门禁与相关测试，并对 manifest、质量报告做初步量化；确认 manifest 是 append/last-wins 日志，原始行数不能当当前数据量。
- 已按 last-wins 重算当前口径并比较报告指纹：当前 2,532 唯一文档、1,011 分配到检索 collection；全部现存质量报告相对当前 manifest 已过期。
- 已读完质量门实现与当前 README/CLI diff；发现门禁报告缺当前 manifest 身份，覆盖/真实回答可彼此一致却共同过期。
- 运行 2026-08-20 fresh quality audit（未覆盖旧报告）：发现 6 篇 collection 污染、5 篇附件依赖缺失、0 active 年度重复、0 日期冲突。
- 已定位 6 篇污染候选与报告指纹：4 篇是当前高价值推免材料附件缺失；现有“通过”总报告使用的评测指纹已落后当前索引，门禁漏洞已复现为真实数据事实。
- 已实跑 current quality gates：5 项失败；另发现 `valid_body_ratio=104.19%` 的分子口径 bug，以及现有错误输出对非专业维护者不可行动。
- 已读现有原子清理与 inbox 实现：清理自带指纹锁、staging 验证和可恢复备份，且会重算 hash；可复用，无需新增数据修复框架。
- 清理 dry-run 通过：1,237 份正文保留、仅 6 份污染候选移出；两个 hash 错误文件均为未跟踪新增数据，内容会被保留并重算 hash。
- 已执行原子清理：保留 1,237 份正文、归档 1,295 条元数据；旧 corpus 可从 `data/.corpus.previous-20260820T031445Z-828718bc` 恢复。
- 清理后验证发现 `fetched_at` 被统一覆盖为迁移日，已暂停后续索引；将从自动备份恢复并修复根因。
- 已恢复旧 corpus、修复迁移保留 `fetched_at`，2 个清理测试通过；重新清理后真实日期分布保持不变，恢复备份为 `data/.corpus.previous-20260820T032024Z-1f79d99b`。
- 已实现 Bearer 保护的管理员 API 与“馆藏账本”Dashboard：当前馆藏、分类、入库日期、筛选、正文、隔离/恢复、体检、受门禁保护的增量发布。
- 用户补充坏答案调试场景后，将需求 steelman 为受控热修闭环：用学生端真实链路复现，显示检索证据，人工答复必须绑定现行可追溯资料，随后只做单条增量并自动复测。
- 已复用 Kimi 既有公众号链路，在 Dashboard 增加单篇链接同步；继续经过公众号白名单、时间窗、相关性、隐私和正文质量门，不新增第二套爬虫。
- 已补真正的文档版本控制：管理员上传与人工标准答复正文采用不可变版本文件；manifest 继续 append-only；Dashboard 可查看旧版并以追加记录方式回退。
- 修复 `ingest_inbox` 手动资料未设置 `index_collection`、因此永远无法进入问答索引的问题。

## 接管续跑（Kimi，2026-08-20 17:20 起）

Codex 因额度在质量门失败处中断；以下为接管后的收口记录。

- 中断现场复核：索引重建本身已完成（manifest/index 指纹一致、test_only=false、BGE-M3/cuda/float16）；失败的是 3 个指纹门禁——`coverage_matches_index`（误把 benchmark-probe 报告当 coverage-audit 报告传入，且指纹落后）、`real_answers_match_index`（答案快照系旧索引 545329… 时代）、`real_answer_integrity`（前两者连带）。
- 重跑 `coverage-audit`（150 题，90 answerable + 60 partial，0 wrong-department）与全量 `answer-benchmark`（不可 resume：快照与索引指纹绑定）。首轮重跑暴露真实退化信号：`core_scene_answerability` 中“奖助学金/就业手续”降至 0.867。
- 定位并修复引用校验误报：公文发文字号（`上财行规[2021]55号`）被 `_CITE_RE` 当作越界引用编号；`_citation_refs` 增加 1900–2099 四位年份豁免，流式门禁与事后校验共用；新增 3 个测试。修复后 citation_issues 2→0。
- 修复 27 篇 UUID/乱码文件名标题的附件（main_qa 内）：标题按“剥离粘连十六进制前缀 → 附件正文首行 → 所属通知父标题+（附件N）”推导，只改 H1、重算 content_hash、追加 manifest（text_hash 与 fetched_at 不动）；增量索引按 embedding_sig 重嵌入这 27 篇（439 chunks）。
- 验证：改标题未翻转两个目标查询的 top-5（就业手续两题仍为检索排序未命中），标题修复作为独立数据质量改进保留，不作为门禁对赌手段。
- 曾误用 `coverage-audit` 默认输出路径覆盖基线文件 `sufe_coverage_before.json/.md`，已用 `git checkout` 恢复（该文件事先无未提交改动，确认无用户内容损失）。
- 检索分解诊断（dense/BM25/RRF 分层排名）：困难认定通知 dense#1+bm25#1 却被 time_weight×boost 重排挤出前列；基层就业附件 dense#1 同样被年度衰减压下——属既有“年度衰减×融合重排”检索问题（本轮冻结检索，不归因于数据）。
- career 源重抓补数：新增 18 篇、更新 23 篇（含 2026 年度各省选调公告、毕业去向登记表单）；2 篇 PII 隔离、1 篇不完整均按规则处理。
- career 补抓引入 5 篇附件引用违规（选调公告正文引用 404/外站/zip·xls 不可解析附件）：`quality-audit` 一致判为 `incomplete+archived`，`rebuild-clean-corpus --apply` 已归档（保留 1271 正文、备份 `data/.corpus.previous-20260820T100618Z-589e8c7d`），`fetched_at` 时间线保真（修复未回归）。
- 补写 README“管理员 Dashboard”章节（codex 计划阶段 6 的维护者指南此前缺失）：令牌配置、概览/治理/问答诊室/标准答复热修/体检与发布的日常闭环。
- 格式归因：`ruff format` 20 个未格式化文件中 16 个为 master 既有、3 个为本任务链新增（admin.py、test_admin.py、test_quality_gates.py 由 codex 改动引入）已格式化；本人新增文件全部格式化。
- 全量回归：pytest 556 passed；ruff check 全绿；admin.js `node --check` 通过。

### 最终质量门（2026-08-20 末轮，index `fe9f1143…`）

14 项过 13 项；唯一红灯 `core_scene_answerability`：就业手续 13/15=0.867（不就业登记、灵活就业登记拒答——均核实为语料确实缺流程级文档的诚实拒答，官方“不就业登记表”页面本身为无正文下载页）。

### 回答质量抽查（不看通过率看内容）

- 优质样例：jwc-score-011（引用 2026 年 4 月最新学籍细则并提示版本新旧）、admit-fail-003（区分申请资格与获资格后挂科取消资格，版本对比清晰）、nic-arrears-004（直接给出 1 小时自动解冻）。
- 弱答 12 题（answered 但开头承认资料不全）：多为检索噪声下的保守回答；career-grassroots-011 为“泛答”——指标算 answered，但对基层就业专项手续仍不够具体。
- 拒答 15 题：抽查 housing-repair-006、medical-location-006、career-flexible-010 等，均核实语料确实无对应内容，拒答正确。
- 域名偏离 answered 9 题：gs-phd-006 等内容实际正确（依据申请考核办法），偏离来自检索混入的就业/教务噪声文档。
- 结论：答案层诚实度健康（宁可拒答/示弱也不编造），主要缺口是就业手续流程级文档与检索排序（本轮冻结检索，未动）。

## 测试结果

| 测试 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 管理端/精编/手动入库定向测试 | 鉴权、热修、增量索引、版本查看/回退、上传边界通过 | 18 passed | passed |
| 质量门/清理/配置定向测试 | 新鲜度门、时间线保真、令牌配置通过 | 15 passed | passed |
| Ruff + admin.js 语法 | 无静态错误 | passed | passed |

## 错误日志

| 时间 | 错误 | 尝试次数 | 解决方案 |
|---|---|---:|---|
| 2026-08-20 | 公众号页面无正文、无可用浏览器实例 | 2 | 查本地抓取记录并尝试只读 HTTP |
| 2026-08-20 | `uv run` 查询解释器缓存路径权限失败 | 1 | 网页解析换系统 Python，后续独立检查项目环境 |
| 2026-08-20 | 浏览器打开但未暴露给会话；精确搜索无结果；HTTP 被微信验证拦截 | 4+ | 改读仓库内引用文章的原始设计文档；保留直读限制 |
| 2026-08-20 | 原子清理覆盖全部文档 `fetched_at` | 1 | 已恢复、修复、重跑并验证日期分布 |
| 2026-08-20 | 更新规划文件时补丁上下文写错文件 | 1 | 用 `rg` 定位实际行后按文件分别更新 |
